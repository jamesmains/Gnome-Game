public enum DamageType {
    Nature,
    Fire,
    Water,
    Holy,
    Dark
}
public enum EntityTeams {
    Player,
    Enemies,
    Environment
}

public enum EntityType {
    Undefined,
    Humanoid,
    Undead
}